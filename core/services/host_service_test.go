package services

import (
	"testing"
)

func TestGetHost(t *testing.T) {
	/*

		// TODO: Find a way to mock the host information

		want := entities.Host{
			Host: "localhost",
			OS:   "Darwin 23.6.0",
			Arch: "arm64",
			Memo: entities.Memo{
				Total:     "1.00 GB",
				Free:      "1.00 GB",
				Used:      "0.00 B",
				Available: "1.00 GB",
			},
		}

		got := GetHost()

		if !reflect.DeepEqual(got, want) {
			t.Errorf("GetHost() = %v, want %v", got, want)
		}
	*/
}
